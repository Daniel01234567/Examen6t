import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { VidPage } from './vid';

@NgModule({
  declarations: [
    VidPage,
  ],
  imports: [
    IonicPageModule.forChild(VidPage),
  ],
})
export class VidPageModule {}
